apply from: "$rootDir/gradle/instrumentation.gradle"

dependencies {
    compileOnly("net.spy:spymemcached:2.12.0")
}

muzzle {
    pass {
        group.set("net.spy")
        module.set("spymemcached")
        versions.set("[2.12.0,)")
        assertInverse.set(true)
    }
}